---
const { label, name, options, required} = Astro.props;
---

<style>
  .form-field__container select {
    width: 100%;
    padding: 0.5rem;
    border: 2px solid var(--form-field-border-color, var(--ink-300));
    color: var(--form-field-input-color, var(--theme-on-bg));
    border-radius: var(--form-field-border-radius, 0.25rem);
    outline-style: none;
    font-size: 1rem;
    font-family: inherit;
    /* Fixes gloss effect on select dropdowns in Safari */
    -webkit-appearance: none;
  }
  .form-field__container select:focus {
    border: 2px solid var(--form-field-border-focus-color, var(--theme-primary));
  }
</style>

<div class="form-field__container">
  {label && <label for={name}>{label}</label>}
  <select id={name} name={name} required={required}>
    <option disabled selected value="">==SELECT AN OPTION==</option>
    {options?.map((option) => (
          <option value={option}>{option}</option>
        ))}
  </select>
</div>


