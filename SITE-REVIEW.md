# phpixel.gr — τι λείπει και τι αξίζει να βελτιωθεί

Έλεγχος ολόκληρου του site μετά την αναδιάρθρωση (Αύγουστος 2026).
Μόνο σημεία. Πες μου ποιο θες να ανοίξω.

## Απόδειξη & εμπιστοσύνη (το μεγαλύτερο κενό)

- Μηδέν πραγματικά testimonials. Υπάρχουν 3 lorem placeholders, κρυμμένα πίσω από `SHOW_TESTIMONIALS`.
- Μηδέν μετρήσιμα αποτελέσματα σε όλα τα 5 case studies (`metrics: []` παντού).
- Καμία δήλωση πελάτη σε κανένα case study.
- Καμία «πριν» εικόνα στο Aporelli, που είναι ακριβώς η δουλειά όπου φαίνεται η διαφορά.
- Καμία φωτογραφία σου. Το /about δείχνει μονόγραμμα ως fallback.
- Κανένα λογότυπο πελάτη πουθενά.
- Κανένα στοιχείο συνεργάτη (`SHOW_TEAM` κλειστό, με placeholder ονόματα).

## Περιεχόμενο

- 2 άρθρα συνολικά, το νεότερο Δεκ 2025. Οκτώ μήνες σιωπής σε site που πουλάει SEO.
- Το `/blog/ab` δείχνει 2 σπασμένες εικόνες: τα `/blog/ab-testing-main.webp` και `/blog/ab-testing-concept-illustration.webp` δεν υπάρχουν.
- Λείπει το `/images/hero-devices.webp` της αρχικής.
- 10 tag pages με 1–2 άρθρα η καθεμία. Thin content.
- Καμία ένδειξη τιμής πουθενά, ούτε εύρος ούτε «από». Το FAQ το αποφεύγει ρητά.
- Κανένα άρθρο δεν έχει `dateModified`.

## Νομικά (το πιο επείγον)

- Δεν υπάρχουν Όροι Χρήσης. Το footer «Νομικά» έχει μόνο την πολιτική απορρήτου.
- Η πολιτική απορρήτου δεν αναφέρει νομική οντότητα, ΑΦΜ, ούτε διεύθυνση. Στην Ελλάδα αυτά είναι υποχρεωτικά.
- Δεν αναφέρει ποιοι τρίτοι επεξεργάζονται δεδομένα (Google Analytics, Resend, Cloudflare) ούτε χρόνο διατήρησης.
- Δεν αναφέρει διαβίβαση δεδομένων εκτός ΕΕ.

## SEO

- Καμία διεύθυνση στο schema, μόνο `addressCountry: GR`. Χωρίς NAP και Google Business Profile δεν υπάρχεις σε τοπικές αναζητήσεις.
- Custom OG εικόνες μόνο στις 6 σελίδες υπηρεσιών. Αρχική, about, portfolio, blog, case studies πέφτουν στο γενικό `/social.png`.
- Κανένα FAQ σε `/services`, `/portfolio`, `/about`. Υπάρχει μόνο σε αρχική και σελίδες υπηρεσιών.
- Τα case studies δεν στοχεύουν keywords, μόνο ονόματα πελατών.
- Κανένα εσωτερικό link από τα άρθρα προς τις σελίδες υπηρεσιών.

## Μετατροπές

- Η φόρμα δεν έχει καμία προστασία από spam: ούτε honeypot, ούτε rate limit, ούτε timestamp check.
- Κανένα σύστημα online ραντεβού, τη στιγμή που το πουλάς ως υπηρεσία (βλ. Αλάζω).
- Καμία εγγραφή newsletter, τη στιγμή που πουλάς email marketing.
- Το τηλέφωνο εμφανίζεται μόνο στο `/contact` (δική σου απόφαση, αλλά κοστίζει κλήσεις από τις σελίδες υπηρεσιών).
- Τα άρθρα τελειώνουν χωρίς CTA.
- Καμία inline επικύρωση στη φόρμα, μόνο browser defaults.

## Προσβασιμότητα

- Τα υπομενού «Υπηρεσίες» στο desktop nav είναι απρόσιτα με πληκτρολόγιο: ανοίγουν μόνο με hover και τα links δεν παίρνουν focus.
- Το κουμπί mobile menu δεν έχει `aria-expanded` ούτε `aria-controls`.
- Το mobile menu δεν παγιδεύει το focus ούτε κλείνει με Escape.
- Το `<nav>` δεν έχει `aria-label`.

## Τεχνικά

- Όλες οι εικόνες ζουν στο `public/`, άρα καμία βελτιστοποίηση από το Astro: χωρίς `srcset`, χωρίς AVIF, χωρίς responsive μεγέθη.
- Τα hero JPG του portfolio είναι βαριά (128–403KB το καθένα).
- Οι γραμματοσειρές έρχονται από Google Fonts, ενώ υπάρχουν ήδη τοπικά woff2 στο `public/assets/fonts/` που δεν χρησιμοποιεί κανείς.
- ~16 αχρησιμοποίητα components του Odyssey theme παραμένουν στο repo (`sections/heros/*`, `TextSection`, `FeatureCard`, `NewsletterForm`, `Logo.astro`, `NetworkAnimation.ts` κ.ά.).
- Κανένα test, κανένα CI, κανένα `astro check` στο build.
- Το GA φορτώνει σε κάθε σελίδα πριν τη συγκατάθεση (το consent mode το μπλοκάρει, αλλά το request γίνεται).

## Εκκρεμότητες του deploy

- Το project στο Vercel πρέπει να απενεργοποιηθεί μετά τη μετάβαση, αλλιώς το ίδιο περιεχόμενο σερβίρεται από δύο hosts.
- Χρειάζονται τα `RESEND_API_KEY`, `CONTACT_TO`, `CONTACT_FROM` στο Cloudflare Pages, αλλιώς η φόρμα πάει πάντα στο `/fail` (βλ. `DEPLOY.md`).
- Το παλιό `sitemap.xml` πρέπει να αντικατασταθεί με `sitemap-index.xml` στο Search Console (το redirect υπάρχει ήδη).
- Υπολείμματα στο repo: `public/loaderio-*.txt`, `.github/FUNDING.yml`, `.idx/dev.nix`.
